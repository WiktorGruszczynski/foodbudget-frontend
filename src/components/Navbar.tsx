export default function Navbar() {
    const options = ["meals", "recipes", "metrics", "premium", "more"];

    return (
            <nav className="h-16 bg-gray-800 text-white flex items-center justify-center">
                abc
            </nav>
    )
}